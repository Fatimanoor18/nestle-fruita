version: '3.8'

services:
  fruita-vitals-app:
    build: .
    container_name: fruita_vitals_website
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./server/db:/app/server/db
    environment:
      - PORT=5000
      - NODE_ENV=production
